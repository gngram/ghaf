[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "devicerouter"
version = "0.1.0"
description = "Host↔GUI-VM device routing over vsock with a PyQt5 UI (+ test-file mode)"
authors = [{ name = "You" }]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
  # PyQt5 is only needed for the GUI. You can also install it manually.
  # "PyQt5>=5.15.0",
]

[project.optional-dependencies]
gui = ["PyQt5>=5.15.0"]

[project.scripts]
devicerouter-gui-app = "devicerouter.main.guest_app:main"
devicerouter-guest-service = "devicerouter.main.guest_service:main"
devicerouter-host-service = "devicerouter.cli.host_service:main"
devicerouter-host-testapp = "devicerouter.cli.host_testapp:main"

[tool.setuptools.packages.find]
where = ["src"]

